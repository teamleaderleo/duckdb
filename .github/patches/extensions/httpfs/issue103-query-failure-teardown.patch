diff --git a/src/s3fs.cpp b/src/s3fs.cpp
--- a/src/s3fs.cpp
+++ b/src/s3fs.cpp
@@ -728,12 +728,13 @@ S3FileHandle::S3FileHandle(FileSystem &fs, const OpenFileInfo &file, FileOpenFl
 }
 
 S3FileHandle::~S3FileHandle() {
-	if (Exception::UncaughtException()) {
-		// We are in an exception, don't do anything
+	auto context = client_context.lock();
+	if (Exception::UncaughtException() || (context && context->IsQueryEndingWithFailure())) {
+		// Failed query teardown must not become a successful multipart publication.
 		return;
 	}
 
 	try {
 		Close();
 	} catch (...) { // NOLINT
 	}
@@ -1114,7 +1116,7 @@ void S3FileHandle::Initialize(optional_ptr<FileOpener> opener) {
 	{
 		lock_guard<mutex> lck(mu);
 		credential_refresh_enabled = refresh_enabled;
-		if (context && refresh_enabled) {
+		if (context) {
 			client_context = context->shared_from_this();
 		} else {
 			client_context.reset();
