# Hive default-partition literal candidate

This carrier validates a writer-only escape for the literal `__HIVE_DEFAULT_PARTITION__` against exact DuckDB source ancestor `de477da7606fc2d857f81117f0140d0550a5c42c`.

The workflow materializes the source and SQLLogic changes at runtime. No direct source claim exists until the native focused run passes.
