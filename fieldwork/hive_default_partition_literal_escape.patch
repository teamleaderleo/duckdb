diff --git a/src/execution/operator/persistent/physical_copy_to_file.cpp b/src/execution/operator/persistent/physical_copy_to_file.cpp
--- a/src/execution/operator/persistent/physical_copy_to_file.cpp
+++ b/src/execution/operator/persistent/physical_copy_to_file.cpp
@@ -2985,8 +2985,14 @@ PartitionDirectory CopyToFileGlobalState::GetPartitionDirectory(const vector<Val
 			p_dir += "=";
 			if (partition_value.IsNull()) {
 				p_dir += "__HIVE_DEFAULT_PARTITION__";
 			} else {
-				p_dir += HivePartitioning::Escape(partition_value.ToString());
+				auto escaped_value = HivePartitioning::Escape(partition_value.ToString());
+				if (escaped_value == "__HIVE_DEFAULT_PARTITION__") {
+					// Keep the raw reserved marker exclusive to SQL NULL. URL decoding
+					// reconstructs the literal value when Hive partitioning reads it.
+					escaped_value = "%5F" + escaped_value.substr(1);
+				}
+				p_dir += escaped_value;
 			}
 			result.path = fs.JoinPath(result.path, p_dir);
 			result.directories.push_back(result.path);
